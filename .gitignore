# Go
/valve-node
*.test
*.out

# Web tooling
cmd/valve-node/web/node_modules/

# goreleaser
/dist/

# OS
.DS_Store

# local debugging artifacts
*.png
.playwright-mcp/

# The compiled control-plane binary. It is a build artifact — CI and
# goreleaser build it from ./cmd/valve-node-app — and tracking it added a
# ~14MB blob to history on almost every commit. Leading slash so this
# matches ONLY the root binary, never the cmd/valve-node-app/ source dir.
/valve-node-app
